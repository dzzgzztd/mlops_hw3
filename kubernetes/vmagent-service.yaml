apiVersion: v1
kind: Service
metadata:
  name: vmagent
  namespace: mlops-hw1
spec:
  selector:
    app: vmagent
  ports:
    - name: http
      port: 8429
      targetPort: 8429
  type: ClusterIP