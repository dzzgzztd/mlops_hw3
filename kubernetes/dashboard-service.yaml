apiVersion: v1
kind: Service
metadata:
  name: ml-dashboard-service
  namespace: mlops-hw1
spec:
  selector:
    app: ml-dashboard
  ports:
    - port: 7860
      targetPort: 7860
  type: NodePort